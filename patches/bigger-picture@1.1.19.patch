diff --git a/package.json b/package.json
index 3e124cb834ed8d908a69ff5fa402da78498a82fb..db05fe77afd3474295d00c02339ae26161f2b57a 100644
--- a/package.json
+++ b/package.json
@@ -75,6 +75,9 @@
 		"sass-build": "sass --style expanded --no-source-map src/demo/_bigger-picture.scss:dist/bigger-picture.css && sass --style compressed --no-source-map src/demo/demo.scss:public/demo.css && cp src/demo/_bigger-picture.scss dist/bigger-picture.scss"
 	},
 	"packageManager": "pnpm@8.12.0",
+	"dependencies": {
+		"body-scroll-lock": "^4.0.0-beta.0"
+	},
 	"devDependencies": {
 		"@rollup/plugin-commonjs": "^25.0.7",
 		"@rollup/plugin-node-resolve": "^15.2.3",
diff --git a/src/bigger-picture.js b/src/bigger-picture.js
index d5f80a27cda1e56d298a4cbc27ed79deeb3695fa..40d600df7b9c7b4a765bc8f525c0e8cbf0a6a20f 100644
--- a/src/bigger-picture.js
+++ b/src/bigger-picture.js
@@ -1,3 +1,4 @@
+import { mount } from 'svelte';
 import BiggerPicture from './bigger-picture.svelte'
 
 /**
@@ -6,7 +7,7 @@ import BiggerPicture from './bigger-picture.svelte'
  * @returns BiggerPicture instance
  */
 export default function (options) {
-	return new BiggerPicture({
+	return mount(BiggerPicture, {
 		...options,
 		props: options,
 	})
diff --git a/src/bigger-picture.svelte b/src/bigger-picture.svelte
index 9d3e47e75ad3addfaeaa8485e03084f21f2c508f..d9e1860d23579cbad826c9ea30ae6e988e7d6787 100644
--- a/src/bigger-picture.svelte
+++ b/src/bigger-picture.svelte
@@ -1,6 +1,7 @@
 <svelte:options accessors={true} immutable={true} />
 
 <script>
+	import { disableBodyScroll, enableBodyScroll } from 'body-scroll-lock'
 	import { fly } from 'svelte/transition'
 	import { cubicOut } from 'svelte/easing'
 	import ImageItem from './components/image.svelte'
@@ -72,10 +73,6 @@
 	export const open = (options) => {
 		opts = options
 		inline = opts.inline
-		// add class to hide scroll if not inline gallery
-		if (!inline && html.scrollHeight > html.clientHeight) {
-			html.classList.add('bp-lock')
-		}
 		// update trigger element to restore focus
 		focusTrigger = document.activeElement
 		container.w = target.offsetWidth
@@ -261,6 +258,10 @@
 		container.el = node
 		let roActive
 		opts.onOpen?.(container.el, activeItem)
+		// disable body scroll if not inline gallery
+		if (!inline) {
+			disableBodyScroll(node)
+		}
 		// don't use keyboard events for inline galleries
 		if (!inline) {
 			window.addEventListener('keydown', onKeydown)
@@ -287,8 +288,8 @@
 				ro.disconnect()
 				window.removeEventListener('keydown', onKeydown)
 				closing.set(false)
-				// remove class hiding scroll
-				html.classList.remove('bp-lock')
+				// Re-enable body scroll
+				enableBodyScroll(node)
 				opts.onClosed?.()
 			},
 		}
